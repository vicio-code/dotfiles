# Don't commit SSH private keys
ssh/.ssh/id_*
ssh/.ssh/*.pem
ssh/.ssh/known_hosts
ssh/.ssh/agent-env

# Don't commit sensitive files
**/secrets
**/*.secret

# Personal/work configs (not tracked)
**/.bash_personal
**/.gitconfig.personal
**/.gitconfig.work
